function solve(arg1, arg2, arg3){
    let length1 = arg1.length;
    let length2 = arg2.length;
    let length3 = arg3.length;

    let sumArgsLength = length1 + length2 + length3;
    let avarege = Math.floor(sumArgsLength / 3);

    console.log(sumArgsLength);
    console.log(avarege);
}